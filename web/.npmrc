# @talqora 作用域走 GitHub Packages;其余包仍走默认 registry(或 Docker build 传入的 npmmirror)。
# 私有包读取需鉴权:通过环境变量 NODE_AUTH_TOKEN 注入(GitHub PAT，权限 read:packages)。
# 本地开发:export NODE_AUTH_TOKEN=<PAT>;Docker 构建:compose 以 build-arg 传入(见 web/Dockerfile)。
@talqora:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
