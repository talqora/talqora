# 系统架构图（模块级）

> 面向零背景读者。先读术语表与图例，再看图。图用 [Mermaid](https://mermaid.js.org) 描述，
> GitHub / VS Code（装 Mermaid 插件）/ mermaid.live 均可渲染。
> 本项目横跨两仓同属一个系统：**our-chat**（IM + WebRTC，含 server/gateway/web/mobile-swift）与
> **agent-server**（RAG + Agent 微服务，独立仓库 `talqora/agent-server`）。

---

## 术语表 / 图例

| 名词 | 大白话解释 |
|---|---|
| **IM** | Instant Messaging，即时通讯（发消息、好友、会话）。 |
| **WebRTC** | 浏览器/App 之间直连传音视频的技术；连不通时经 TURN 服务器中继。 |
| **STUN/TURN（coturn）** | 帮两端穿透 NAT（家用路由器的地址转换）建立音视频连接；穿透失败就由 TURN 中继媒体流。 |
| **信令（signaling）** | 通话前双方交换"我在哪、用什么编码"的控制消息；本项目走业务后端。 |
| **Socket.io** | 一个基于 WebSocket 的实时通讯库（server 用它做实时推送）。 |
| **原生 WebSocket** | 浏览器标准的长连接协议（gateway 用它，不带 Socket.io 那层封装）。 |
| **长连接 / 网关（gateway）** | 客户端与服务器保持不断开的连接，用于实时推送；gateway 是专门扛这类连接的 Go 服务。 |
| **backplane（背板）** | 多个网关实例之间用 Redis 发布订阅互相转发消息，保证"连在不同实例上的两个人也能收到对方消息"。 |
| **presence（在线状态）** | 谁在线/离线/正在输入。 |
| **RAG** | 检索增强生成：先从你上传的知识库里检索相关片段，再让大模型据此回答。 |
| **Agent / 任务编排** | 让大模型按步骤调用工具完成任务；run-engine 记录每步事件。 |
| **向量库（Milvus）** | 把文本转成数字向量后存起来，支持"语义相似检索"（RAG 的检索靠它）。 |
| **BullMQ** | 基于 Redis 的任务队列；把耗时活（文档摄取、agent 任务）丢队列由 worker 异步跑。 |
| **JWKS** | JSON Web Key Set，一组验签公钥。server 是身份签发方（IdP），下游用它的 JWKS 公钥验证 token 真伪。 |
| **BFF** | Backend For Frontend，为前端服务的后端；这里指 server 帮 web 铸 agent-server 用的 token。 |
| **oc-shared** | 两仓容器互通用的 Docker 外部网络（agent-server 经它访问 server 的 JWKS；nginx 经它反代到 agent）。 |
| **契约类型包** | `@talqora/agent-contracts`：agent-server 的 proto 生成的 TS 类型，发布到 GitHub Packages 供 web 消费。 |

**连线含义**：实线箭头 = 运行时调用/数据流；虚线 = 构建期/契约依赖。

---

## 图 0 · 系统总览（客户端 → 边缘 → 服务 → 基础设施）

```mermaid
flowchart TB
    subgraph clients["客户端"]
        web["Web（React SPA）"]
        ios["iOS（Swift / TCA）"]
    end

    subgraph edge["边缘（our-chat web 容器内 nginx）"]
        nginx["nginx 反向代理 + 静态托管"]
    end

    subgraph ocnet["Docker 网络"]
      subgraph ourchat["our-chat 后端"]
          server["server（Express）<br/>HTTP API + Socket.io + OAuth IdP"]
          gateway["gateway（Go）<br/>原生 WebSocket 长连接"]
      end
      subgraph agentsrv["agent-server（独立仓库）"]
          nodeServer["node-server（NestJS）<br/>HTTP + SSE"]
          nodeWorker["node-worker（NestJS）<br/>BullMQ 消费者"]
      end
    end

    subgraph infra_oc["our-chat 基础设施"]
        pgOC[("PostgreSQL<br/>库 our_chat")]
        redisOC[("Redis<br/>Socket.io适配器/presence/网关背板/限流/OAuth存储")]
        coturn["coturn（STUN/TURN）"]
        s3[("对象存储 S3/COS<br/>上传文件")]
    end

    subgraph infra_ag["agent-server 基础设施"]
        pgAG[("PostgreSQL<br/>库 agent_server")]
        redisAG[("Redis<br/>BullMQ 队列")]
        milvus[("Milvus 向量库")]
        etcd[("etcd<br/>Milvus 元数据")]
        cosAG[("COS<br/>Milvus 对象存储 + 文档/产出")]
        llm["LLM 服务<br/>千问 DashScope（OpenAI 兼容）"]
    end

    web -->|"HTTPS 静态 + /api /oauth /user /socket.io"| nginx
    ios -->|"HTTPS REST（OpenAPI）"| nginx

    nginx -->|"/api /oauth /user /.well-known /socket.io"| server
    nginx -->|"/ws（原生 WebSocket）"| gateway
    nginx -->|"/agent/ 经 oc-shared"| nodeServer
    nginx -->|"/ 返回 SPA 静态"| web

    web -.->|"WebRTC 媒体 P2P / 经 TURN 中继"| coturn
    ios -.->|"WebRTC 媒体"| coturn

    server --> pgOC
    server --> redisOC
    server --> s3
    server -->|"发通话信令"| coturn
    gateway --> redisOC

    nodeServer --> pgAG
    nodeServer --> redisAG
    nodeServer --> milvus
    nodeServer --> cosAG
    nodeServer -->|"embedding / chat"| llm
    nodeWorker --> pgAG
    nodeWorker --> redisAG
    nodeWorker --> milvus
    nodeWorker --> cosAG
    nodeWorker -->|"embedding / chat"| llm
    milvus --> etcd
    milvus --> cosAG
    nodeServer <-->|"入队/出队 runs"| redisAG
    nodeWorker <-->|"消费 runs"| redisAG

    agentsrv -.->|"取 JWKS 公钥验签<br/>/.well-known/jwks.json 经 oc-shared"| server
    web -.->|"铸 agent token（BFF）<br/>/oauth/agent-token"| server
    nodeServer -.->|"发布契约类型包<br/>@talqora/agent-contracts"| web
```

---

## 图 1 · our-chat / server（Express）模块级

```mermaid
flowchart TB
    subgraph server["server（Node + Express + Prisma + Socket.io）"]
        entry["app.ts / server.ts<br/>HTTP + Socket.io 启动"]

        subgraph routes["routes（HTTP 路由）"]
            rLogin["login.ts"]
            rRegister["register.ts"]
            rUser["user.ts"]
            rFriend["friend.ts"]
            rChat["chat.ts"]
            rSync["sync.ts"]
            rRead["（read via services）"]
            rTurn["turn.ts（TURN 凭据）"]
            rUpload["upload.ts / uploadAdvanced.ts"]
            rInternal["internal.ts（网关回调）"]
        end

        subgraph services["services（业务逻辑）"]
            sMessage["message.ts"]
            sRead["read.ts（已读回执）"]
            sCall["callSession.ts（通话会话/信令）"]
        end

        subgraph realtime["realtime（Socket.io）"]
            rtPresence["presence.ts"]
            rtPush["push.ts（消息扇出）"]
        end

        subgraph oauth["oauth（OAuth2 / OIDC 身份签发 IdP）"]
            oAuthorize["authorize.ts"]
            oToken["token.ts / tokens.ts"]
            oJwks["jwks.ts / keys.ts"]
            oDisc["discovery.ts"]
            oIntro["introspect.ts / revoke.ts"]
            oUserinfo["userinfo.ts"]
            oPkce["pkce.ts"]
            oAgent["agentToken.ts（BFF 铸 agent token）"]
            oClients["clients.ts / storage.ts / audit.ts"]
        end

        subgraph mw["middleware"]
            mAuth["auth.ts"]
            mRate["rateLimit.ts"]
        end

        subgraph data["database / storage"]
            dPrisma["prisma.ts"]
            dRedis["redis.ts"]
            dBigint["bigint-json.ts"]
            stStorage["storage.ts（S3/COS）"]
        end

        contracts["contracts/gen（proto→TS 类型）"]
        config["config / utils / types"]
    end

    entry --> routes
    entry --> realtime
    entry --> oauth
    routes --> mw
    routes --> services
    services --> realtime
    services --> data
    realtime --> dRedis
    oauth --> data
    rUpload --> stStorage
    routes -. 出入参类型 .-> contracts

    dPrisma --> PG[("PostgreSQL")]
    dRedis --> RD[("Redis")]
    stStorage --> S3[("S3/COS")]
    oJwks -.->|"公钥供下游验签"| downstream["gateway / agent-server"]
```

---

## 图 2 · gateway（Go）模块级

```mermaid
flowchart TB
    subgraph gw["gateway（Go 1.22 + gorilla/websocket）"]
        cmd["cmd/gateway/main.go（启动装配）"]
        subgraph internal["internal"]
            ws["ws/server.go<br/>升级 HTTP→WebSocket、握手鉴权"]
            auth["auth/jwt.go<br/>JWT 验签（用 server 的 JWKS）"]
            hub["hub/hub.go + conn.go<br/>连接注册表、读写泵、背压"]
            presence["presence/presence.go<br/>在线状态"]
            backplane["backplane/backplane.go<br/>Redis 发布订阅跨实例转发"]
            upstream["upstream/upstream.go<br/>回调 server 业务接口"]
            metrics["metrics/metrics.go<br/>Prometheus 指标"]
            config["config/config.go"]
            gwContracts["contracts/gen（proto→Go）"]
        end
    end

    client["客户端 /ws"] --> ws
    ws --> auth
    auth -.->|"取公钥"| serverJWKS["server /.well-known/jwks.json"]
    ws --> hub
    hub --> presence
    hub <--> backplane
    backplane <--> Redis[("Redis 背板")]
    hub --> upstream
    upstream --> server["server（业务/信令）"]
    hub -. 消息类型 .-> gwContracts
    ws --> metrics
```

---

## 图 3 · web（React）模块级

```mermaid
flowchart TB
    subgraph web["web（React + Redux Toolkit + antd + socket.io-client + i18next）"]
        main["main.tsx / App.tsx"]
        router["router（路由）"]

        subgraph views["views（页面）"]
            vAuth["authView（登录/注册）"]
            vChat["chatView（会话/消息/通话）"]
            vDir["directoryView（通讯录）"]
            vMe["meView / settingView"]
            vAgent["agentView（RAG/Agent，直连 agent-server）"]
            vLayout["layout / debug"]
        end

        subgraph store["store（Redux 状态）"]
            stRoot["rootStore.ts"]
            stUser["userStore.ts"]
            stChat["chatStore.ts"]
            stFriend["friendStore.ts"]
            stCall["callStore.ts（WebRTC 状态）"]
        end

        subgraph infra["前端基础设施"]
            api["globalApi（axios 封装）"]
            comps["globalComponents"]
            gtype["globalType"]
            hooks["hooks"]
            i18n["i18n / locales"]
            rum["rum.ts（前端监控）"]
            wContracts["contracts/gen（our-chat proto→TS）"]
        end

        agentApi["views/agentView/api.ts + agentAuth.ts"]
        pkg["@talqora/agent-contracts（外部类型包）"]
    end

    main --> router --> views
    views --> store
    views --> infra
    store --> api
    api -->|"/api REST"| server["server"]
    stChat -.->|"socket.io-client /socket.io"| server
    stCall -.->|"信令 via socket.io + WebRTC"| server
    vAgent --> agentApi
    agentApi -->|"/agent/api REST + SSE"| agentServer["agent-server"]
    agentApi -->|"先铸 token /oauth/agent-token"| server
    vAgent -. 类型 .-> pkg
    views -. our-chat 域类型 .-> wContracts
```

---

## 图 4 · mobile-swift（iOS）模块级

```mermaid
flowchart TB
    subgraph ios["mobile-swift（Swift + TCA + Tuist）"]
        subgraph features["Features（TCA 功能模块）"]
            fRoot["Root / Main"]
            fAuth["Auth"]
            fChats["Chats / ChatDetail"]
            fContacts["Contacts / NewFriends / Search"]
            fDiscover["Discover / Me"]
        end
        subgraph servicesL["Services（领域服务）"]
            svAuth["Auth"]
            svChat["Chat"]
            svFriend["Friend"]
        end
        subgraph core["Core"]
            cNet["Networking"]
            cAuth["Auth"]
            cKeychain["Keychain"]
            cUtil["Util"]
        end
        contractsL["Contracts/OpenAPI（openapi.yaml 生成的 Codable 客户端）"]
        ds["DesignSystem"]
        models["Models"]
    end

    features --> servicesL
    servicesL --> core
    servicesL --> contractsL
    cNet --> contractsL
    contractsL -->|"REST（OpenAPI）经 nginx /api"| server["server"]
    cAuth -->|"OAuth 登录 /oauth"| server
    features --> ds
```

---

## 图 5 · agent-server（NestJS）模块级（双进程：HTTP + Worker）

```mermaid
flowchart TB
    subgraph agent["agent-server / apps/node-server（NestJS + BullMQ + Milvus）"]
        subgraph proc["进程（同镜像不同入口）"]
            mServer["main.ts → node-server（HTTP/SSE）"]
            mWorker["main.worker.ts → node-worker（BullMQ 消费）"]
        end

        subgraph modules["modules（业务）"]
            modAuth["auth（JWKS 验签守卫）"]
            modAgent["agent（agent-runner + tool.registry）"]
            modConv["conversations（chat.service：RAG 问答）"]
            modDoc["documents（parser + ingestion + text-splitter）"]
            modRuns["runs（run.processor：BullMQ 消费入口）"]
            modTask["task-sessions（任务会话归组）"]
            modHealth["health"]
        end

        subgraph shared["shared（基础能力）"]
            shLLM["llm（OpenAI 兼容 / 千问）"]
            shRag["rag（rag.retriever 检索）"]
            shMilvus["milvus（向量存取）"]
            shQueue["queue（BullMQ 连接/队列）"]
            shRunEngine["run-engine（事件溯源）"]
            shPrisma["prisma（PostgreSQL）"]
            shRedis["redis"]
        end

        contractsA["contracts/gen（proto→TS）<br/>→ 发布 @talqora/agent-contracts"]
    end

    mServer --> modules
    mWorker --> modRuns
    modAuth -.->|"取 JWKS 公钥"| serverJWKS["our-chat server<br/>/.well-known/jwks.json"]
    modConv --> shRag --> shMilvus
    modConv --> shLLM
    modDoc -->|"入队摄取任务"| shQueue
    modAgent -->|"入队 agent 任务"| shQueue
    modRuns --> shRunEngine
    modRuns --> modDoc
    modRuns --> modAgent
    shQueue <--> Redis[("Redis / BullMQ")]
    modDoc --> shLLM
    modDoc --> shMilvus
    shPrisma --> PG[("PostgreSQL agent_server")]
    shMilvus --> Milvus[("Milvus + etcd + COS")]
    modules -. 类型 .-> contractsA
```

---

## 图 6 · 关键运行时流程（时序）

### 6.1 发消息 + 多实例实时扇出
```mermaid
sequenceDiagram
    participant A as 发送方
    participant NG as nginx
    participant S as server（Express）
    participant DB as PostgreSQL
    participant R as Redis（Socket.io 适配器）
    participant B as 接收方
    A->>NG: POST /api/chat 发消息
    NG->>S: 转发
    S->>DB: 持久化消息
    S->>R: 通过 Redis 适配器广播（跨实例）
    R-->>S: 投递到接收方所在实例
    S-->>B: Socket.io 推送新消息
    S-->>A: 200（含消息回执）
```

### 6.2 WebRTC 通话（信令 + TURN 中继兜底）
```mermaid
sequenceDiagram
    participant A as 主叫
    participant S as server（callSession 信令）
    participant T as coturn（TURN）
    participant B as 被叫
    A->>S: offer（经 Socket.io 信令）
    S-->>B: 转发 offer
    B->>S: answer + ICE 候选
    S-->>A: 转发 answer + ICE
    A-->>B: 尝试 P2P 直连
    Note over A,B: 直连失败（对称 NAT/跨网）时
    A->>T: 经 TURN 中继媒体流
    T->>B: 中继转发
```

### 6.3 跨服务鉴权（server 签发 → agent-server 验签）
```mermaid
sequenceDiagram
    participant W as web（agentView）
    participant S as server（OAuth IdP / BFF）
    participant AG as agent-server
    W->>S: 已登录会话 → 请求 /oauth/agent-token
    S-->>W: 铸一枚 RS256 的 agent-scoped token
    W->>AG: 带 token 调 /agent/api（经 nginx）
    AG->>S: 取 /.well-known/jwks.json 公钥（经 oc-shared）
    AG->>AG: 用公钥验签 token（无需询问 server）
    AG-->>W: 返回业务数据 / SSE 流
```

### 6.4 Agent / RAG（文档摄取 + 检索问答）
```mermaid
sequenceDiagram
    participant W as web
    participant NS as node-server（HTTP/SSE）
    participant Q as Redis/BullMQ
    participant NW as node-worker
    participant ML as Milvus
    participant L as LLM（千问）
    W->>NS: 上传文档
    NS->>Q: 入队"摄取"任务，返回 runId
    NW->>Q: 消费摄取任务
    NW->>NW: 解析 → 分块（text-splitter）
    NW->>L: 生成 embedding 向量
    NW->>ML: 写入向量
    W->>NS: 提问（对话）
    NS->>L: 问题 embedding
    NS->>ML: 语义检索相关分块
    NS->>L: 拼上下文让 LLM 生成答案
    NS-->>W: SSE 流式返回答案 + 引用（Citation）
```

---

## 附：数据存储归属一览

| 存储 | 归属 | 存什么 |
|---|---|---|
| PostgreSQL `our_chat` | our-chat/server | 用户、好友、会话、消息、OAuth 客户端/授权 |
| PostgreSQL `agent_server` | agent-server | 文档、会话、run、task-session |
| Redis（our-chat） | server + gateway | Socket.io 适配器广播、presence、网关背板、限流、OAuth 临时态 |
| Redis（agent-server） | agent-server | BullMQ 队列（runs） |
| Milvus（+etcd+COS） | agent-server | 知识库分块向量 |
| S3/COS（our-chat） | server | 用户上传的图片/文件 |
| COS / 本地卷（agent-server） | agent-server | 原始文档、agent 产出、Milvus 对象存储 |

> 契约依赖（构建期）：agent 域类型的权威是 agent-server 的 proto，经 `@talqora/agent-contracts`
> 发布给 web 消费；our-chat 自身域（read/message/presence/call）的 proto 在 our-chat 仓库，
> 生成 server（TS）与 gateway（Go）两端类型。详见 `docs/技术方案` 与 agent-server `docs/SOP`。
