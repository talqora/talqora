# iOS 知识库助手小程序 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** iOS 原生「知识库助手」小程序:home 下拉入口 → 首次一键授权(IdP)→ 3-tab 功能页(对话 SSE 流式 / 文档 / agent 任务),后端复用已就绪的 agent-server。

**Architecture:** SwiftUI+TCA。新增 `Services/Agent`(SSE 解析、agent 鉴权 token 管理、agent REST+SSE 客户端)与 `Features/MiniApp`(入口面板、授权门、3-tab 助手)。鉴权走 our-chat 首方端点 `POST /oauth/agent-token`;agent API base = `<origin>/agent/api`。iOS 首次为网络层引入 SSE(`URLSession.bytes`,可带 Bearer 头)。

**Tech Stack:** Swift 6 严格并发 · iOS 18 · Tuist · TCA(@Reducer/@ObservableState/@DependencyClient/TestStore)· SwiftUI · Swift Testing · URLSession(REST+multipart+bytes/SSE)。

**Spec:** `docs/superpowers/specs/2026-07-05-ios知识库助手小程序-design.md`

**分支:** `ios`。**约束**:每步代码+测试同提交;动 UI 前加载 `mobile-ios-design`+`swiftui-pro` skill,守 mobile-swift/CLAUDE.md UX 硬规范;新文件后 `cd mobile-swift && tuist generate --no-open`;完工门禁 `tuist generate && xcodebuild build`(+ 相关测试);模拟器名 **"iPhone 17 Pro"**;提交中文 `type(scope):msg` 无署名;**用户 Xcode 构建期间不抢 DerivedData**。

**关键后端事实(定论):**
- 授权:`POST <origin>/oauth/agent-token`,Header `Authorization: Bearer <主App登录JWT>`,无 body → `{access_token, token_type:"Bearer", expires_in:900}`(15min,RS256,aud=agent-server)。Bearer 免 CSRF。
- agent base:`<origin>/agent/api`(生产 `https://tujiang.tech/agent/api`)。所有 agent 请求 `Authorization: Bearer <agent-token>`。
- 端点:`GET /auth/me`;`GET/POST /conversations`;`GET/DELETE /conversations/:id`;`POST /conversations/:id/messages{query,topK?}`(SSE);`GET/POST/DELETE /documents`(POST multipart→`{documentId,runId}`);`POST /agent/tasks{task}`→202`{runId}`;`GET /runs/:runId/stream`(SSE,Last-Event-ID);`GET /runs/:runId`。
- 对话 SSE 事件:`event:token data:{"type":"token","value":"x"}` / `event:done data:{"type":"done","messageId":N,"citations":[{"chunkId","documentId","score"}]}` / `event:error data:{"type":"error","message":"..."}`。

**先决探明(执行者做一次)**:`APIEnvironment.current.baseURLString` 现为 prod `https://tujiang.tech`。iOS 主 App 登录 token 在 Keychain(key `accessToken`),经 `AuthenticatedAPIClient` 以 `Authorization: Bearer` 注入(见 `Sources/Core/Networking/AuthenticatedAPIClient.swift`)。Keychain 封装见 `Sources/Core/Keychain/KeychainStore.swift`(service `com.ourchat.ios.tokens`,`TokenKey` 枚举)。

---

## File Structure

```
mobile-swift/Sources/
  Services/Agent/
    SSEParser.swift            # 纯函数:字节/字符串 → SSE 帧 [{event,data}]
    AgentDTO.swift             # Codable/Sendable DTO + SSE 事件解码
    AgentEnv.swift             # base 拼接:agentBase = <origin>/agent/api
    AgentAuthClient.swift      # @DependencyClient:authorize/ensureToken/isAuthorized/clear + TokenCache
    AgentAPIClient.swift       # @DependencyClient:request(JSON)/upload(multipart)/stream(SSE)
  Core/Keychain/KeychainStore.swift   # 改:TokenKey 加 .agentToken
  Features/MiniApp/
    MiniAppFeature.swift + MiniAppView.swift         # 容器:授权门 ↔ 助手 切换
    MiniAppLauncher.swift                            # home 下拉面板 + 入口
    AgentAuthFeature.swift + AgentAuthView.swift     # 授权页
    KnowledgeAssistantFeature.swift + KnowledgeAssistantView.swift  # TabView 3 tab
    Chat/AgentChatFeature.swift + AgentChatListView.swift + AgentChatDetailView.swift
    Documents/AgentDocumentsFeature.swift + AgentDocumentsView.swift
    Tasks/AgentTasksFeature.swift + AgentTasksView.swift
  Features/Chats/ChatsView.swift   # 改:顶部下拉手势 → 唤起 launcher
  Features/Main/MainFeature.swift + MainView.swift  # 改:@Presents miniApp + fullScreenCover
mobile-swift/Tests/Agent/…         # SSEParser / DTO / TokenCache / 各 reducer 测试
```

---

## Milestone 1 — 地基(SSE / DTO / 鉴权 / 客户端)

### Task 1: SSEParser(纯函数,TDD)

**Files:** Create `mobile-swift/Sources/Services/Agent/SSEParser.swift`; Test `mobile-swift/Tests/Agent/SSEParserTests.swift`

- [ ] **Step 1: 失败测试**
```swift
import Testing
@testable import OurChat

struct SSEParserTests {
    @Test func parsesSingleEvent() {
        var p = SSEParser()
        let frames = p.consume("event: token\ndata: {\"v\":1}\n\n")
        #expect(frames.count == 1)
        #expect(frames[0].event == "token")
        #expect(frames[0].data == "{\"v\":1}")
    }
    @Test func buffersHalfFrameThenCompletes() {
        var p = SSEParser()
        #expect(p.consume("event: done\n").isEmpty)          // 半包不出帧
        let frames = p.consume("data: ok\n\n")
        #expect(frames.count == 1 && frames[0].event == "done" && frames[0].data == "ok")
    }
    @Test func multipleEventsInOneChunk() {
        var p = SSEParser()
        let frames = p.consume("event: a\ndata: 1\n\nevent: b\ndata: 2\n\n")
        #expect(frames.map(\.event) == ["a", "b"])
    }
    @Test func multiLineDataJoinedWithNewline() {
        var p = SSEParser()
        let frames = p.consume("data: line1\ndata: line2\n\n")
        #expect(frames[0].data == "line1\nline2")
    }
}
```
- [ ] **Step 2: 运行确认失败** — `cd mobile-swift && tuist generate --no-open && xcodebuild test -workspace OurChat.xcworkspace -scheme OurChat -destination 'platform=iOS Simulator,name=iPhone 17 Pro' -only-testing:OurChatTests/SSEParserTests` → FAIL(未定义)。
- [ ] **Step 3: 实现**
```swift
import Foundation

struct SSEFrame: Equatable, Sendable { var event: String; var data: String }

// 增量喂入字符串块,按 SSE 规范(空行分帧,event:/data: 字段,data 多行以 \n 连接)吐出完整帧。
// 半包安全:未遇空行的内容留在缓冲区。
struct SSEParser {
    private var buffer = ""
    mutating func consume(_ chunk: String) -> [SSEFrame] {
        buffer += chunk
        var frames: [SSEFrame] = []
        while let range = buffer.range(of: "\n\n") {
            let block = String(buffer[buffer.startIndex..<range.lowerBound])
            buffer.removeSubrange(buffer.startIndex..<range.upperBound)
            if let f = Self.parseBlock(block) { frames.append(f) }
        }
        return frames
    }
    private static func parseBlock(_ block: String) -> SSEFrame? {
        var event = "message"; var dataLines: [String] = []
        for line in block.split(separator: "\n", omittingEmptySubsequences: false) {
            let l = String(line)
            if l.hasPrefix("event:") { event = l.dropFirst(6).trimmingCharacters(in: .whitespaces) }
            else if l.hasPrefix("data:") { dataLines.append(String(l.dropFirst(5).trimmingCharacters(in: .whitespaces))) }
        }
        if dataLines.isEmpty { return nil }
        return SSEFrame(event: event, data: dataLines.joined(separator: "\n"))
    }
}
```
- [ ] **Step 4: 运行确认通过** — 同 Step 2,4 tests PASS。
- [ ] **Step 5: Commit** — `git add mobile-swift/Sources/Services/Agent/SSEParser.swift mobile-swift/Tests/Agent/SSEParserTests.swift && git commit -m "feat(小程序): SSE 帧解析器(纯函数,半包/多事件/多行 data)"`

---

### Task 2: Agent DTO + SSE 事件解码(TDD 解码)

**Files:** Create `mobile-swift/Sources/Services/Agent/AgentDTO.swift`; Test `mobile-swift/Tests/Agent/AgentDTOTests.swift`

- [ ] **Step 1: 失败测试**
```swift
import Testing
import Foundation
@testable import OurChat

struct AgentDTOTests {
    @Test func decodesTokenResponse() throws {
        let d = Data(#"{"access_token":"jwt","token_type":"Bearer","expires_in":900}"#.utf8)
        let r = try JSONDecoder().decode(AgentTokenResponse.self, from: d)
        #expect(r.accessToken == "jwt" && r.expiresIn == 900)
    }
    @Test func decodesChatTokenEvent() throws {
        let e = try ChatStreamEvent.decode(event: "token", data: #"{"type":"token","value":"你"}"#)
        #expect(e == .token("你"))
    }
    @Test func decodesChatDoneEvent() throws {
        let e = try ChatStreamEvent.decode(event: "done", data: #"{"type":"done","messageId":5,"citations":[{"chunkId":1,"documentId":2,"score":0.8}]}"#)
        if case let .done(id, cites) = e { #expect(id == 5 && cites.first?.documentId == 2) } else { Issue.record("wrong case") }
    }
}
```
- [ ] **Step 2: 运行确认失败** — `-only-testing:OurChatTests/AgentDTOTests` → FAIL。
- [ ] **Step 3: 实现**
```swift
import Foundation

struct AgentTokenResponse: Codable, Equatable, Sendable {
    var accessToken: String, tokenType: String, expiresIn: Int
    enum CodingKeys: String, CodingKey { case accessToken = "access_token", tokenType = "token_type", expiresIn = "expires_in" }
}
struct AgentUser: Codable, Equatable, Sendable { var id: Int; var username: String; var displayName: String? }
struct Citation: Codable, Equatable, Sendable { var chunkId: Int; var documentId: Int; var score: Double }
struct AgentMessage: Codable, Equatable, Sendable, Identifiable {
    var id: Int; var role: String; var content: String; var citations: [Citation]?
}
struct Conversation: Codable, Equatable, Sendable, Identifiable {
    var id: Int; var title: String; var messages: [AgentMessage]?
}
struct AgentDocument: Codable, Equatable, Sendable, Identifiable {
    var id: Int; var filename: String; var size: Int?; var chunkCount: Int?; var status: String; var error: String?
}
struct UploadResult: Codable, Equatable, Sendable { var documentId: Int; var runId: String }
struct RunIdResult: Codable, Equatable, Sendable { var runId: String }

// 对话 SSE:token 增量 / done(带引用)/ error
enum ChatStreamEvent: Equatable, Sendable {
    case token(String), done(messageId: Int, citations: [Citation]), error(String)
    static func decode(event: String, data: String) throws -> ChatStreamEvent {
        let d = Data(data.utf8)
        switch event {
        case "token":
            struct T: Decodable { let value: String }
            return .token(try JSONDecoder().decode(T.self, from: d).value)
        case "done":
            struct De: Decodable { let messageId: Int; let citations: [Citation]? }
            let x = try JSONDecoder().decode(De.self, from: d); return .done(messageId: x.messageId, citations: x.citations ?? [])
        default:
            struct E: Decodable { let message: String? }
            return .error((try? JSONDecoder().decode(E.self, from: d).message) ?? "生成失败")
        }
    }
}

// 任务/文档 run SSE:够用即可(名称 + 原始 data),UI 侧再细分。
struct RunEvent: Equatable, Sendable { var name: String; var data: String }
```
> 字段名(role/citations/status 等)以 agent-server 真实响应为准:实现前用一次真实 `GET /agent/api/conversations` 核对,如与上表不符则按真实字段微调 DTO + 测试。
- [ ] **Step 4: 运行确认通过** — 3 tests PASS。
- [ ] **Step 5: Commit** — `git commit -m "feat(小程序): agent DTO + 对话 SSE 事件解码"`

---

### Task 3: Keychain 加 agentToken + AgentEnv

**Files:** Modify `mobile-swift/Sources/Core/Keychain/KeychainStore.swift`; Create `mobile-swift/Sources/Services/Agent/AgentEnv.swift`

- [ ] **Step 1: Keychain 加 key** — 在 `TokenKey` 枚举加 `case agentToken`(读现有枚举匹配写法)。
- [ ] **Step 2: AgentEnv**
```swift
import Foundation
enum AgentEnv {
    // agent-server 经 nginx /agent/ 反代;base = <主站 origin>/agent/api。
    static var base: String { APIEnvironment.current.baseURLString + "/agent/api" }
}
```
- [ ] **Step 3: build 确认** — `xcodebuild build ...` SUCCEEDED。
- [ ] **Step 4: Commit** — `git commit -m "feat(小程序): Keychain agentToken key + AgentEnv base 拼接"`

---

### Task 4: AgentAuthClient(token 取/缓存/续期,TDD 缓存逻辑)

**Files:** Create `mobile-swift/Sources/Services/Agent/AgentAuthClient.swift`; Test `mobile-swift/Tests/Agent/AgentTokenCacheTests.swift`

- [ ] **Step 1: 失败测试(缓存/过期判定抽成纯结构)**
```swift
import Testing
import Foundation
@testable import OurChat

struct AgentTokenCacheTests {
    @Test func validWhenNotNearExpiry() {
        let c = AgentTokenCache(token: "t", expiresAt: Date(timeIntervalSinceNow: 120))
        #expect(c.isValid(now: Date(), skew: 30) == true)
    }
    @Test func invalidWithinSkew() {
        let c = AgentTokenCache(token: "t", expiresAt: Date(timeIntervalSinceNow: 20))
        #expect(c.isValid(now: Date(), skew: 30) == false)
    }
}
```
- [ ] **Step 2: 运行确认失败** — `-only-testing:OurChatTests/AgentTokenCacheTests` → FAIL。
- [ ] **Step 3: 实现**
```swift
import Dependencies
import DependenciesMacros
import Foundation

struct AgentTokenCache: Equatable, Sendable {
    var token: String; var expiresAt: Date
    func isValid(now: Date, skew: TimeInterval) -> Bool { expiresAt.timeIntervalSince(now) > skew }
}

@DependencyClient
struct AgentAuthClient: Sendable {
    var authorize: @Sendable () async throws -> Void       // 首次:mint + 置已授权标记
    var ensureToken: @Sendable () async throws -> String   // 有效缓存则复用,否则静默 re-mint
    var isAuthorized: @Sendable () -> Bool = { false }
    var clear: @Sendable () -> Void
}

extension AgentAuthClient: DependencyKey {
    static let liveValue: AgentAuthClient = {
        let store = AgentTokenStore()
        return AgentAuthClient(
            authorize: { _ = try await store.mintAndPersist() },
            ensureToken: { try await store.ensure() },
            isAuthorized: { store.isAuthorized() },
            clear: { store.clear() }
        )
    }()
    static let previewValue = AgentAuthClient(
        authorize: {}, ensureToken: { "preview-token" }, isAuthorized: { true }, clear: {}
    )
}
extension DependencyValues { var agentAuth: AgentAuthClient { get { self[AgentAuthClient.self] } set { self[AgentAuthClient.self] = newValue } } }

// 进程内 token 管理:非 Sendable 状态用 actor 隔离;mint 用主 App 登录态调 /oauth/agent-token。
private actor AgentTokenStore {
    private var cache: AgentTokenCache?
    private var inflight: Task<String, Error>?

    func ensure() async throws -> String {
        if let c = cache, c.isValid(now: Date(), skew: 30) { return c.token }
        if let t = inflight { return try await t.value }
        let task = Task { try await mint() }
        inflight = task
        defer { inflight = nil }
        return try await task.value
    }
    func mintAndPersist() async throws -> String {
        let t = try await mint(); setAuthorizedFlag(true); return t
    }
    private func mint() async throws -> String {
        @Dependency(\.keychain) var keychain
        guard let login = (try? keychain.load(.accessToken)) ?? nil else { throw AgentAuthError.noSession }
        var req = URLRequest(url: URL(string: APIEnvironment.current.baseURLString + "/oauth/agent-token")!)
        req.httpMethod = "POST"
        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
        req.setValue("Bearer \(login)", forHTTPHeaderField: "Authorization")
        let (data, resp) = try await URLSession.shared.data(for: req)
        guard (resp as? HTTPURLResponse)?.statusCode == 200 else { throw AgentAuthError.mintFailed }
        let r = try JSONDecoder().decode(AgentTokenResponse.self, from: data)
        cache = AgentTokenCache(token: r.accessToken, expiresAt: Date(timeIntervalSinceNow: TimeInterval(r.expiresIn)))
        return r.accessToken
    }
    // 已授权标记 + agent token 落 Keychain(可选;内存缓存已足够,标记决定是否跳过授权页)
    private func isAuthorizedFlag() -> Bool { UserDefaults.standard.bool(forKey: "agent.authorized") }
    func isAuthorized() -> Bool { isAuthorizedFlag() }
    private func setAuthorizedFlag(_ v: Bool) { UserDefaults.standard.set(v, forKey: "agent.authorized") }
    func clear() { cache = nil; setAuthorizedFlag(false) }
}
enum AgentAuthError: Error { case noSession, mintFailed }
```
> `isAuthorized`/`clear` 的同步闭包读 UserDefaults(线程安全);token 缓存在 actor 内。若 Swift6 对 actor 方法在同步闭包调用报错,则把标记读写移出 actor 到一个 `nonisolated` 小类型。实现时以编译为准。
- [ ] **Step 4: 运行确认通过** — 2 tests PASS;build SUCCEEDED。
- [ ] **Step 5: Commit** — `git commit -m "feat(小程序): AgentAuthClient(/oauth/agent-token 取+缓存+静默续期+已授权标记)"`

---

### Task 5: AgentAPIClient(REST + multipart + SSE)

**Files:** Create `mobile-swift/Sources/Services/Agent/AgentAPIClient.swift`

- [ ] **Step 1: 实现接口 + liveValue**
```swift
import Dependencies
import DependenciesMacros
import Foundation

struct AgentRequest: Sendable {
    var method: String; var path: String; var body: Data?
    static func get(_ p: String) -> AgentRequest { .init(method: "GET", path: p, body: nil) }
    static func post(_ p: String, _ body: Data? = nil) -> AgentRequest { .init(method: "POST", path: p, body: body) }
    static func delete(_ p: String) -> AgentRequest { .init(method: "DELETE", path: p, body: nil) }
}

@DependencyClient
struct AgentAPIClient: Sendable {
    var request: @Sendable (_ req: AgentRequest) async throws -> Data
    var upload: @Sendable (_ fileURL: URL, _ fileName: String) async throws -> UploadResult
    var stream: @Sendable (_ req: AgentRequest) -> AsyncThrowingStream<SSEFrame, Error> = { _ in .finished }
}

extension AgentAPIClient: DependencyKey {
    static let liveValue = AgentAPIClient(
        request: { req in
            @Dependency(\.agentAuth) var auth
            let token = try await auth.ensureToken()
            var r = URLRequest(url: URL(string: AgentEnv.base + req.path)!)
            r.httpMethod = req.method
            r.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
            if let b = req.body { r.httpBody = b; r.setValue("application/json", forHTTPHeaderField: "Content-Type") }
            let (data, resp) = try await URLSession.shared.data(for: r)
            let code = (resp as? HTTPURLResponse)?.statusCode ?? 0
            guard (200..<300).contains(code) else { throw AgentAPIError.http(code) }
            return data
        },
        upload: { fileURL, fileName in
            @Dependency(\.agentAuth) var auth
            let token = try await auth.ensureToken()
            var r = URLRequest(url: URL(string: AgentEnv.base + "/documents")!)
            r.httpMethod = "POST"
            r.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
            let boundary = "Boundary-\(UUID().uuidString)"
            r.setValue("multipart/form-data; boundary=\(boundary)", forHTTPHeaderField: "Content-Type")
            var body = Data()
            let fileData = try Data(contentsOf: fileURL)
            body.append("--\(boundary)\r\n".data(using: .utf8)!)
            body.append("Content-Disposition: form-data; name=\"file\"; filename=\"\(fileName)\"\r\n".data(using: .utf8)!)
            body.append("Content-Type: application/octet-stream\r\n\r\n".data(using: .utf8)!)
            body.append(fileData)
            body.append("\r\n--\(boundary)--\r\n".data(using: .utf8)!)
            let (data, resp) = try await URLSession.shared.upload(for: r, from: body)
            guard (resp as? HTTPURLResponse)?.statusCode == 201 || (resp as? HTTPURLResponse)?.statusCode == 200 else { throw AgentAPIError.http((resp as? HTTPURLResponse)?.statusCode ?? 0) }
            return try JSONDecoder().decode(UploadResult.self, from: data)
        },
        stream: { req in
            AsyncThrowingStream { continuation in
                let task = Task {
                    do {
                        @Dependency(\.agentAuth) var auth
                        let token = try await auth.ensureToken()
                        var r = URLRequest(url: URL(string: AgentEnv.base + req.path)!)
                        r.httpMethod = req.method
                        r.setValue("Bearer \(token)", forHTTPHeaderField: "Authorization")
                        r.setValue("text/event-stream", forHTTPHeaderField: "Accept")
                        if let b = req.body { r.httpBody = b; r.setValue("application/json", forHTTPHeaderField: "Content-Type") }
                        let (bytes, resp) = try await URLSession.shared.bytes(for: r)
                        guard (200..<300).contains((resp as? HTTPURLResponse)?.statusCode ?? 0) else { throw AgentAPIError.http((resp as? HTTPURLResponse)?.statusCode ?? 0) }
                        var parser = SSEParser()
                        for try await line in bytes.lines {
                            for frame in parser.consume(line + "\n") { continuation.yield(frame) }
                        }
                        continuation.finish()
                    } catch { continuation.finish(throwing: error) }
                }
                continuation.onTermination = { _ in task.cancel() }
            }
        }
    )
    static let previewValue = AgentAPIClient(request: { _ in Data() }, upload: { _, _ in .init(documentId: 0, runId: "") }, stream: { _ in .finished })
}
extension DependencyValues { var agentAPI: AgentAPIClient { get { self[AgentAPIClient.self] } set { self[AgentAPIClient.self] = newValue } } }
enum AgentAPIError: Error, Equatable { case http(Int) }
```
> SSE 用 `bytes.lines` 逐行 + `SSEParser`;注意 `bytes.lines` 去掉了换行,故补 `+ "\n"` 再喂解析器(解析器靠 `\n\n` 分帧,空行=空 line)。若实测分帧不准,改用 `bytes`(逐字节)喂 `String(decoding:)`。以真实流验证为准。
- [ ] **Step 2: build 确认** — SUCCEEDED。
- [ ] **Step 3: 冒烟(可选,需真机/能连 agent)** — 临时在某处调 `agentAPI.request(.get("/auth/me"))` 确认授权链路通(base+token 正确)。
- [ ] **Step 4: Commit** — `git commit -m "feat(小程序): AgentAPIClient(REST+multipart+SSE via URLSession.bytes,自动 Bearer)"`

---

## Milestone 2 — 入口 + 授权门

### Task 6: AgentAuthFeature + AgentAuthView(授权页)

**Files:** Create `Features/MiniApp/AgentAuthFeature.swift` + `AgentAuthView.swift`; Test `Tests/Agent/AgentAuthFeatureTests.swift`

- [ ] **Step 1: 失败测试(reducer)**
```swift
import ComposableArchitecture
import Testing
@testable import OurChat

@MainActor
struct AgentAuthFeatureTests {
    @Test func authorizeSuccessEmitsAuthorized() async {
        let store = TestStore(initialState: AgentAuthFeature.State()) { AgentAuthFeature() } withDependencies: {
            $0.agentAuth.authorize = {}
        }
        await store.send(.authorizeTapped) { $0.isLoading = true }
        await store.receive(\.authorized) { $0.isLoading = false }
    }
    @Test func authorizeFailureShowsError() async {
        let store = TestStore(initialState: AgentAuthFeature.State()) { AgentAuthFeature() } withDependencies: {
            $0.agentAuth.authorize = { throw AgentAuthError.mintFailed }
        }
        await store.send(.authorizeTapped) { $0.isLoading = true }
        await store.receive(\.authorizeFailed) { $0.isLoading = false; $0.errorMessage = "授权失败,请重试" }
    }
}
```
- [ ] **Step 2: 运行确认失败** → FAIL。
- [ ] **Step 3: 实现 reducer**
```swift
import ComposableArchitecture

@Reducer
struct AgentAuthFeature {
    @ObservableState struct State: Equatable { var isLoading = false; var errorMessage: String? }
    enum Action { case authorizeTapped, authorized, authorizeFailed, delegate(Delegate)
        enum Delegate { case authorized } }
    @Dependency(\.agentAuth) var agentAuth
    var body: some ReducerOf<Self> {
        Reduce { state, action in
            switch action {
            case .authorizeTapped:
                state.isLoading = true; state.errorMessage = nil
                return .run { send in try await agentAuth.authorize(); await send(.authorized) }
                    catch: { _, send in await send(.authorizeFailed) }
            case .authorized:
                state.isLoading = false
                return .send(.delegate(.authorized))
            case .authorizeFailed:
                state.isLoading = false; state.errorMessage = "授权失败,请重试"; return .none
            case .delegate: return .none
            }
        }
    }
}
```
- [ ] **Step 4: 运行确认通过** → 2 tests PASS。
- [ ] **Step 5: AgentAuthView(加载 skill + 守 UX 规范)** — 说明文案 + 「一键授权」按钮(≥44pt,`PressableButtonStyle`,loading 禁用)+ 错误态可重试;`WeChatColor/Font` 令牌;`@Bindable var store`。build + Preview。
- [ ] **Step 6: Commit** — `git commit -m "feat(小程序): 授权页(AgentAuthFeature 一键授权 + View)"`

---

### Task 7: MiniAppFeature 容器 + KnowledgeAssistant 壳

**Files:** Create `Features/MiniApp/MiniAppFeature.swift` + `MiniAppView.swift` + `KnowledgeAssistantFeature.swift` + `KnowledgeAssistantView.swift`

- [ ] **Step 1: MiniAppFeature(授权门 ↔ 助手切换)**
```swift
@Reducer
struct MiniAppFeature {
    @ObservableState struct State: Equatable {
        var authorized: Bool = false
        var auth = AgentAuthFeature.State()
        var assistant = KnowledgeAssistantFeature.State()
    }
    enum Action { case onAppear, auth(AgentAuthFeature.Action), assistant(KnowledgeAssistantFeature.Action) }
    @Dependency(\.agentAuth) var agentAuth
    var body: some ReducerOf<Self> {
        Scope(state: \.auth, action: \.auth) { AgentAuthFeature() }
        Scope(state: \.assistant, action: \.assistant) { KnowledgeAssistantFeature() }
        Reduce { state, action in
            switch action {
            case .onAppear: state.authorized = agentAuth.isAuthorized(); return .none
            case .auth(.delegate(.authorized)): state.authorized = true; return .none
            default: return .none
            }
        }
    }
}
```
- [ ] **Step 2: MiniAppView** — `if store.authorized { KnowledgeAssistantView } else { AgentAuthView }`,`.onAppear{ store.send(.onAppear) }`,全屏、顶部有关闭按钮(退出小程序)。
- [ ] **Step 3: KnowledgeAssistantFeature/View 壳** — `State` 含 `selectedTab` + 三个子 feature state(先只放 chat,documents/tasks 里程碑 4/5 接);`KnowledgeAssistantView` = 底部 `TabView`(对话/知识库/任务,SF Symbols + 令牌)。先放 chat tab,其余占位"即将上线"。
- [ ] **Step 4: build + Preview** — SUCCEEDED。
- [ ] **Step 5: Commit** — `git commit -m "feat(小程序): MiniApp 容器(授权门↔助手)+ 3-tab 壳"`

---

### Task 8: home 下拉入口 + MainFeature 呈现

**Files:** Modify `Features/Chats/ChatsView.swift`, `Features/Main/MainFeature.swift` + `MainView.swift`; Create `Features/MiniApp/MiniAppLauncher.swift`

- [ ] **Step 1: MainFeature 加呈现** — `@Presents var miniApp: MiniAppFeature.State?` + `Action.miniApp(PresentationAction<MiniAppFeature.Action>)` + `Action.openMiniApp` → `state.miniApp = .init()`;`.ifLet(\.$miniApp, action: \.miniApp){ MiniAppFeature() }`;`MainView` 加 `.fullScreenCover(item: $store.scope(state: \.miniApp, action: \.miniApp)) { MiniAppView(store: $0) }`。
- [ ] **Step 2: MiniAppLauncher 面板** — 一个下拉展开的面板视图(参考微信小程序区:标题「小程序」+ 一个"知识库助手"图标磁贴),点磁贴 → 回调唤起。
- [ ] **Step 3: ChatsView 顶部下拉** — 在 `List`/容器加下拉手势(如 `.refreshable` 之外的自定义 DragGesture,或滚动到顶继续下拉阈值)→ 展开 MiniAppLauncher(sheet 或内联覆盖)→ 点入口 → 冒泡 delegate 到 Main → `.openMiniApp`。**保持简单**:可用一个下拉出现的浮层 + 点击进入。
- [ ] **Step 4: build + 手动**(模拟器)确认下拉能出面板、点击能全屏进 MiniApp(授权页)。
- [ ] **Step 5: Commit** — `git commit -m "feat(小程序): home 下拉入口面板 + MainFeature 全屏呈现 MiniApp"`

---

## Milestone 3 — 对话 tab(主线)

### Task 9: AgentChatFeature(会话列表 + 详情 + SSE 流式)

**Files:** Create `Features/MiniApp/Chat/AgentChatFeature.swift` + `AgentChatListView.swift` + `AgentChatDetailView.swift`; Test `Tests/Agent/AgentChatFeatureTests.swift`

- [ ] **Step 1: 失败测试(核心:发消息→token 流→done)**
```swift
import ComposableArchitecture
import Testing
@testable import OurChat

@MainActor
struct AgentChatFeatureTests {
    @Test func sendStreamsTokensThenDone() async {
        let (stream, cont) = AsyncThrowingStream<SSEFrame, Error>.makeStream()
        var state = AgentChatFeature.State(); state.currentConversationId = 1
        state.input = "你好"
        let store = TestStore(initialState: state) { AgentChatFeature() } withDependencies: {
            $0.agentAPI.stream = { _ in stream }
        }
        await store.send(.sendTapped) {
            $0.input = ""
            $0.messages.append(AgentMessage(id: -1, role: "user", content: "你好", citations: nil))
            $0.messages.append(AgentMessage(id: -2, role: "assistant", content: "", citations: nil))
            $0.isStreaming = true
        }
        cont.yield(SSEFrame(event: "token", data: #"{"type":"token","value":"在"}"#))
        await store.receive(\.streamEvent) { $0.messages[1].content = "在" }
        cont.yield(SSEFrame(event: "done", data: #"{"type":"done","messageId":9,"citations":[]}"#))
        await store.receive(\.streamEvent) { $0.messages[1].id = 9; $0.isStreaming = false }
        cont.finish()
        await store.finish()
    }
}
```
- [ ] **Step 2: 运行确认失败** → FAIL。
- [ ] **Step 3: 实现 reducer**(会话列表 `load`/`create`/`delete` 走 `agentAPI.request`;发消息走 `agentAPI.stream`,逐帧 `ChatStreamEvent.decode` → 追加/固定/回滚)。`Action`:`onAppear/loadConversations/conversationsLoaded/createTapped/select(id)/messagesLoaded/inputChanged/sendTapped/streamEvent(ChatStreamEvent)/streamFailed/...`。`streamEvent(.token(v))` → 末条 assistant 追加;`.done(id,cites)` → 固定 id + citations + isStreaming=false;`.error` → 回滚为错误态。发送 effect:`.run{ for try await f in agentAPI.stream(.post("/conversations/\(id)/messages", body)) { await send(.streamEvent(try ChatStreamEvent.decode(event:f.event,data:f.data))) } } catch: { streamFailed }`。
- [ ] **Step 4: 运行确认通过** → PASS(补 loadConversations/create/delete 各一条 TestStore 测试)。
- [ ] **Step 5: UI(加载 skill,守 UX)** — List 视图(会话列表三态,新建/删除)+ 详情(消息气泡:user 右/assistant 左,流式逐字,引用角标可点看来源;底部输入条 + 发送,`safeAreaInset`,收键盘);令牌/44pt/PressableButtonStyle;Preview 各态。
- [ ] **Step 6: Commit** — `git commit -m "feat(小程序): 对话 tab(会话列表 + SSE 流式问答 + 引用)"`

---

## Milestone 4 — 知识库(文档)tab

### Task 10: AgentDocumentsFeature + View

**Files:** Create `Features/MiniApp/Documents/AgentDocumentsFeature.swift` + `AgentDocumentsView.swift`; Test `Tests/Agent/AgentDocumentsFeatureTests.swift`

- [ ] **Step 1: 失败测试** — TestStore:`onAppear → loadDocuments`(mock `agentAPI.request` 返回文档 JSON)→ `documentsLoaded` 填列表三态;`delete(id)` → 调 request + 移除。
- [ ] **Step 2: 运行确认失败** → FAIL。
- [ ] **Step 3: 实现 reducer** — `load`(GET /documents 解析 `[AgentDocument]`)、`upload`(选文件 → `agentAPI.upload` → 拿 runId → 轮询 `load` 刷新状态,`@Dependency(\.continuousClock)` 每 5s 直到 ready/failed)、`delete`(DELETE /documents/:id)。三态。
- [ ] **Step 4: 运行确认通过** → PASS。
- [ ] **Step 5: UI** — 文档列表(文件名 + 大小 + 状态徽章:解析中/就绪/失败,三态;`AsyncStateView`)+ 顶部「上传」(`.fileImporter` 选 PDF)+ 滑动删除。令牌/UX 规范。
- [ ] **Step 6: 接入 TabView** — KnowledgeAssistantFeature 的"知识库"tab 换成 AgentDocumentsView。
- [ ] **Step 7: Commit** — `git commit -m "feat(小程序): 知识库(文档)tab(列表三态 + 上传轮询 + 删除)"`

---

## Milestone 5 — 任务 tab

### Task 11: AgentTasksFeature + View

**Files:** Create `Features/MiniApp/Tasks/AgentTasksFeature.swift` + `AgentTasksView.swift`; Test `Tests/Agent/AgentTasksFeatureTests.swift`

- [ ] **Step 1: 失败测试** — TestStore:`submitTapped`(mock `agentAPI.request` POST /agent/tasks 返回 `{runId}`)→ `runStarted(runId)`;受控 SSE 注入 `tool_called`/`final_answer` → 事件累积 + 终态。
- [ ] **Step 2: 运行确认失败** → FAIL。
- [ ] **Step 3: 实现 reducer** — `submit`(POST /agent/tasks → runId)→ 订阅 `agentAPI.stream(.get("/runs/\(runId)/stream"))`,逐帧存 `RunEvent`(name+data);`final_answer`/`run_failed` → 终态。断线用 `GET /runs/:id` 快照兜底(可选)。
- [ ] **Step 4: 运行确认通过** → PASS。
- [ ] **Step 5: UI** — 任务输入 + 提交;事件时间线(tool_called/tool_result 折叠 `DisclosureGroup`;final_answer 高亮)。令牌/UX。
- [ ] **Step 6: 接入 TabView** — "任务"tab 换成 AgentTasksView。
- [ ] **Step 7: Commit** — `git commit -m "feat(小程序): 任务 tab(提交 + runs SSE 工具过程 + 结果)"`

---

## Milestone 6 — 打磨与验收

### Task 12: 打磨 + 全量回归

- [ ] **Step 1: 全套单测 + build** — `xcodebuild test ...`(全 scheme)全绿;`swiftui-pro` review 三个 tab + 授权页 + launcher,修 deprecated API / 令牌 / 一类一文件 / 无障碍。
- [ ] **Step 2: UX 自检**(mobile-swift/CLAUDE.md §6)— 44pt/按下反馈/三态/明暗/安全区/Dynamic Type;下拉入口手感;收键盘。
- [ ] **Step 3: Commit** — `git commit -m "refactor(小程序): swiftui-pro review 修正 + UX 自检"`

### Task 13: 真机端到端验收(需用户)

- [ ] **Step 1** — 用户真机 Clean Build 到设备(连 prod agent-server)。
- [ ] **Step 2** — 逐条验收 spec §1:下拉入口 → 一键授权 → 对话流式+引用 → 文档上传/状态 → 任务过程/结果;token 续期无感;登出失效提示。
- [ ] **Step 3** — 记录问题,建后续修复 task。

---

## Self-Review(计划自检)

- **Spec 覆盖**:§3.1 授权→Task4;§3.2 API+SSE→Task2/5;§5.1 入口→Task8;§5.2 鉴权→Task4;§5.3 网络SSE→Task1/5;§5.4 对话/文档/任务→Task9/10/11;§4 容器/授权门→Task6/7;§7 错误三态→各 tab + Task12;§8 测试→各 Task 单测;成功标准→Task13。无遗漏。
- **占位**:DTO 字段名以真实响应为准(Task2 注明冒烟核对,非占位);SSE `bytes.lines` 分帧以真实流验证(Task5 注明),这是"该类代码的正确验证方式"而非占位。
- **类型一致**:`SSEFrame`/`ChatStreamEvent`/`AgentTokenCache`/`AgentAPIClient.stream`/`AgentAuthClient.ensureToken` 命名跨任务一致。
- **风险**:agent 真实响应字段(role/status/citations)与 run SSE 事件名需在 Task2/5/11 用真实请求核对后微调 DTO——已在对应任务显式标注。
